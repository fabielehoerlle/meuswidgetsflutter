import 'package:flutter/material.dart';

class Romantico extends StatelessWidget {
  const Romantico({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
